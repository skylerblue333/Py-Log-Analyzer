# Py-Log-Analyzer

Apache/Nginx access log parser and analyzer with error rate, top IPs, and top paths.

## Quick Start

```bash
pip install -r requirements.txt
pytest tests/ -v
uvicorn src.main:app --reload
```
